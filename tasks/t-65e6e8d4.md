---
id: t-65e6e8d4
title: P7: signatures support — fetch GET /signatures, apply per-mailbox signature in compose
status: done
added: 2026-09-04
---

## Description

New upstream 1.3.4 feature (Signature/SignatureCandidates/SignatureSelection/SignatureSnapshot schemas). Depends on P1 regen. Investigate server semantics in ~/Developer/hqbase at v1.3.4 before designing.

## Plan



## Artifacts

- Commit 3d99c49 on feat/upstream-134-adoption: signatures end to end (DTOs, GET /signatures, selection on draft/send/reply/forward, compose menu + preview, snapshot cached on CachedDraft).
- Verified server contract appended to `.memory/architecture/HQBase Mail API v1 Contract.md` ("Signatures (upstream 1.3.4) — verified 2026-09-04").
- Decision note `.memory/decisions/Herald Signature Handling.md`.
- Tests: HeraldKit/Tests/HeraldKitTests/SignatureTests.swift (15), HeraldTests/ComposeSignatureTests.swift (9), plus a cache round-trip test in DraftSyncTests and signature resolution in the HeraldKit FakeMailAPIClient. HeraldKit 233 tests pass, app 222 pass, build-detached.sh succeeds.
- Spec divergence to re-apply after any openapi regen: `Draft` allOf[0] points at a Herald-local `DraftFields` (DraftInput minus the write-only `signature`), because upstream's shape fails to decode a draft whose signature was deleted (`{"mode":"selected","id":null}`).
- Not done: no live check against a running 1.3.4 server (local instance was down); Herald does not manage (create/edit/delete) signatures — that is a web-app surface.

