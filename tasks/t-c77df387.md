---
id: t-c77df387
title: 1.4 F1: sync/labels fixes — drain labelsChanged on failure, label mutation fence, tests, SyncEngine split
status: done
added: 2026-09-19
priority: high
---

## Description

Audit report documents/reports/audit-2026-09-19-hqbase-142-adoption.md items C1, C2, P4, P5, P6, P12 + SyncEngine.swift split under 1000 lines. Files: SyncEngine(+Labels,+Drafts), MailStore, MailStore+Labels, MailActionService, MailViewModel+Labels (P12 only), HeraldKit Sync tests.

## Plan



## Artifacts

Commit 1d3a614 on branch worktree-agent-a4f0ddb8203b7bd0f.

- C1: `SyncEngine.runPass` drains `passLabelsChanged` on every exit (defer + explicit call before each `.failed`, so the order is chips-then-banner).
- C2: label fence pinning `(accountID, messageID, labelID)` in `MailStore.pinnedLabels`; honoured by `writeMessageAssignments` (embedded writes) and `replaceAssignments` (the sweep), bypassed by the action's own settle; released on all six exits of both `MailActionService.setLabel` methods; cascaded by the delete paths and `deleteAll`.
- P4: `embeddingIsRedecidedOnRestart` — `start()` clears `labelEmbeddingAccounts`; the test also documents that a graph reinstall without stop/start is a NEW engine and so re-decides by construction.
- P5: `actionAnswersCarryMembership` — a triage answer's embedded labels reach the store; a `labels: nil` answer changes nothing.
- P6: `productionClientAsksForLabels` source guard in SourceBoundaryTests (requires exactly one production `HQBaseAPIClient(` site and asserts `includeLabels: true`).
- P12: `updateLabelSurfaceVisibility` records the flag only after the engine hop, and returns early when `sync` is nil.
- Split: SyncEngine.swift 1352 → 996; new SyncEngine+Labels.swift (345) and SyncEngine+Drafts.swift (74). Verified declaration-for-declaration against the pre-split file.

Tests: HeraldKit 347 → 356, app-hosted 276, both green; grep guards clean. Every new test mutation-checked.

Memory updated: "Herald Label Caching and UI Architecture" (fence design, A3 race narrowed not fixed) and "Herald Sync Model".

Not done / for other agents: AppEnvironment.swift split (F3, another agent); the pre-existing flaky `OutboxServiceTests."Two concurrent first-saves of one draft create exactly one server draft"` seen once on the baseline run (Compose area, unrelated).

