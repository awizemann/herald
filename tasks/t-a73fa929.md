---
id: t-a73fa929
title: Analytics P3: instrument AppEnvironment/MailViewModel/ComposeViewModel + lifecycle
status: done
added: 2026-08-18
priority: high
---

## Description

AppEnvironment owns `usage: any UsageTracking` (built in init via UsageAnalytics.makeTracker with the plist key; NoopUsageTracker default for tests) and `record(_ event: UsageEvent)` chaining each call on the previous Task so events reach the SDK in order. Pass tracker/record into MailViewModel (per account) and ComposeViewModel. Hook every approved event at the action methods (not views): MailViewModel+Actions perform/performOnSelection/toggleStar/toggleRead/saveAttachment/requestCompose; sync finished/failed with trigger; search; drafts; ComposeViewModel saveNow/send/discard; AppEnvironment signIn/reauthenticate/signOut/selectedAccountID/notificationsSettingChanged/setMailboxColorToken/launch_failed; UpdateService explicit check. view_shown: in MailViewModel.selection didSet + showDrafts + openThread/exitThread; consume a pending `via` source BEFORE the equality guard; emit launch view explicitly after first load. Lifecycle: from the existing NSApplication didBecomeActive/didResignActive observer → applicationDidBecomeActive() / flush(). Store-hook tests with a recording actor tracker (deadline-based drain; trailing sentinel when asserting fires-once), incl. didSet no double-fire and via-consumption. Blast radius: Herald/App/AppEnvironment.swift, MailViewModel*.swift, Compose/ComposeViewModel.swift, App/UpdateService.swift, HeraldTests. Depends on P1.

## Plan



## Artifacts

AppEnvironment (usage + record chain + lifecycle), HeraldApp, MailViewModel(+Actions,+Drafts), ComposeViewModel, AttachmentSaver.Outcome, UpdateService/MailCommands, SidebarView; HeraldTests/Analytics/UsageInstrumentationTests.swift. 158 tests green. Deviations: count always .one (single-selection list); non-MailAPIError failures not recorded (no `other` kind); .shortcut via unreachable.

