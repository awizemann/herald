<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Herald</title>
        <item>
            <title>0.4.1</title>
            <pubDate>Sun, 06 Sep 2026 11:37:57 -0400</pubDate>
            <sparkle:version>9</sparkle:version>
            <sparkle:shortVersionString>0.4.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h3>Fixed</h3>
<ul>
<li>Sign-in can no longer trap you. A sign-in that stops making progress — most often because macOS never opens the browser window it was asked for — now shows a <strong>Cancel</strong> button on both the first-run screen and the Add Account sheet, and Cancel abandons the sign-in itself rather than just closing the window over it. The screen is usable again immediately and a second attempt works — and a cancelled sign-in is really cancelled: the account it had already stored is removed and its token revoked, rather than quietly reappearing at the next launch. The re-auth banner offers the same Cancel while it is signing you back in. Herald #9.</li>
<li>The spinner now says what it is waiting for: contacting your server, checking this Mac&#x27;s registration, registering, waiting for the sign-in window, completing sign-in, saving credentials, setting up your mailbox. Each step is logged too, so a stuck sign-in names its location instead of just spinning.</li>
<li>A browser sign-in that never reports back no longer waits forever: it is noted in the log after 45 seconds and fails with an explanation after ten minutes. The deadline is deliberately long — macOS gives Herald no signal that the window actually opened, so a shorter one would close a consent page you were still typing into. Cancel is the fast way out.</li>
<li>Sign-in and token requests have real deadlines (15s per request, 30s overall). They previously inherited the system default of seven days, so a server that accepted the connection and then went quiet could hang the sign-in indefinitely.</li>
<li>Keychain reads and writes during sign-in and at launch no longer run on the main thread, so a stalled system keychain leaves Herald responsive — and cancellable — instead of frozen.</li>
<li>Signing out now clears the cached server discovery for that address. Signing back into a server that was reinstalled in the meantime, without quitting Herald, could previously reuse the old install&#x27;s endpoints.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/awizemann/herald/releases/download/v0.4.1/Herald-0.4.1.zip" length="3584628" type="application/octet-stream" sparkle:edSignature="Pl8e9nvRyW08cO5TMWyWkLh0Mrq2DpYXtN64+lfS2hdpZrdlAm+VqyLtoFv9YtnKopqpgokWSoJanlsnul0UAQ=="/>
        </item>
    </channel>
</rss>