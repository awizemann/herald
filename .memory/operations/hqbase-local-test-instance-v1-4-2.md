---
title: HQBase Local Test Instance (v1.4.2)
type: note
permalink: hqbase-mac/operations/hqbase-local-test-instance-v1-4-2
created: 2026-09-04
updated: 2026-09-19
---

## Observations
- [fact] Local HQBase test instance runs entirely offline via `pnpm dev` (= `pnpm build && wrangler dev --port 8787`) in a git worktree of ~/Developer/hqbase checked out at a version tag; all bindings (D1, R2, Durable Object, Queue) run as wrangler local simulators, no Cloudflare account/credentials needed. Setup: `pnpm install`, write `.dev.vars` with BETTER_AUTH_SECRET + HQBASE_LOCAL_SEED_PASSWORD (random values, kept only in `.dev.vars`, never in memory/chat), `pnpm db:migrate:local`, `pnpm db:seed:local` (seeds owner@hqbase.test), then `pnpm dev` in the background until GET http://localhost:8787/api/v1/openapi.json answers 200 #setup #procedure
- [fact] CURRENT instance verified 2026-09-19: worktree at scratchpad/hqbase-142 (tag v1.4.2, commit a1161f1), created via `git -C ~/Developer/hqbase worktree add <scratchpad-path>/hqbase-142 v1.4.2`. `pnpm db:migrate:local` applies 4 grouped migrations (0001-0004, wrapping the individual .sql files under migrations/) cleanly from empty; `pnpm db:seed:local` seeds 8 mailboxes + 100 messages with no schema-specific changes needed vs the 1.3.4 procedure. GET /api/v1/openapi.json returns 200 with `/api/v1/signatures/manage` present and `includeLabels` in the GET /messages parameter list #verified-142
- [fact] Send-guard triggers exist at 1.4.2 (`SELECT name FROM sqlite_schema WHERE type='trigger'` via `npx wrangler d1 execute DB --local --command`): `drafts_before_{update,delete}_pending_send`, `draft_attachments_before_{update,delete,insert}_pending_send`, `draft_labels_before_{update,delete,insert}_pending_send`. Confirmed live: `POST /api/v1/send` with an `idempotencyKey` returns 201, not 503 SEND_STORAGE_NOT_READY #send-guard-142
- [gotcha] Keep this test worktree SEPARATE from ~/Developer/hqbase itself, that checkout is shared with another live session and must not be disturbed or committed in; use `git worktree add <path> <tag>` instead of checking out the tag in place. Production hqbase.alanwizemann.com is a separate deployed worker, untouched by any of this — the test instance is local-only wrangler dev #isolation #scope
- [fact] Driving PKCE by curl against `/api/auth/oauth2/*` (better-auth + @better-auth/oauth-provider): sign-in `POST /api/auth/sign-in/email` (cookie jar); register client `POST /api/auth/oauth2/register` with `application_type:"native"`, native redirect URI, `resources:["{origin}/api/v1"]`; `GET /api/auth/oauth2/authorize?...&resource=...` 302s to `/oauth/consent?...` (or straight to the redirect_uri with a code if that client+user already consented); POST the same query string back as `oauth_query` to `POST /api/auth/oauth2/consent` with `{accept:true, oauth_query:"<raw query string, no leading ?>"}` — this call and `POST /api/auth/sign-out` both require an explicit `Origin: http://localhost:8787` header or 403 MISSING_OR_NULL_ORIGIN; consent 200 returns `{redirect:true,url:"<scheme>:/callback?code=...&state=..."}`; exchange at `POST /api/auth/oauth2/token` (form-encoded) with `code_verifier` + `resource` #curl-pkce

## Relations
- relates_to [[HQBase Mail API v1 Contract]]



## 1.4.0 compatibility instance
- [fact] SECOND local instance stood up 2026-09-19 for Herald compat testing, at tag v1.4.0 (commit 9e1aedc), worktree `/private/tmp/claude-501/-Users-awizemann-Developer-hqbase-mac/1a18ce05-0603-47fa-8085-9e57b95749d6/scratchpad/hqbase-140`, port 8788, running alongside (not replacing) the 1.4.2 instance on port 8787/scratchpad/hqbase-142. Setup identical to the 1.4.2 procedure: `git -C ~/Developer/hqbase worktree add <path> v1.4.0`, `pnpm install`, fresh random `.dev.vars` (BETTER_AUTH_SECRET via `openssl rand -hex 32`, HQBASE_LOCAL_SEED_PASSWORD via `openssl rand -base64 24` filtered to alnum, never echoed/pasted anywhere), `pnpm db:migrate:local` (4 migrations 0001-0004 applied cleanly from empty), `pnpm db:seed:local` (8 mailboxes/100 messages, owner@hqbase.test). D1/R2/DO state lives under the worktree's own `.wrangler/state`, fully isolated from the 8787 instance. #setup-140
- [fact] Started via `pnpm build` then `pnpm exec wrangler dev --port 8788` directly (backgrounded), NOT the repo's `pnpm dev` script — that script hardcodes wrangler to port 8787 and also spawns a vite dev server on 5173, which would collide with the already-running 1.4.2 instance. Running wrangler dev alone (skipping vite) is sufficient for API compat testing. #procedure-140
- [fact] Verified 2026-09-19: GET /api/v1/openapi.json → 200, does NOT contain `/api/v1/signatures/manage` and has no `includeLabels` parameter on GET /messages — 1.4.0 predates both the signature-management endpoints and label-on-message support that 1.4.2 has. GET /.well-known/oauth-protected-resource/api/v1 → 200 with `scopes_supported: ["mail:read","mail:write","mail:send"]` — no `signatures:manage` scope at this tag. #verified-140
- [fact] Full PKCE flow via curl against the 8788 instance (same recipe as 1.4.2's #curl-pkce, one deviation): client registration's `redirect_uris` must be a reverse-domain URI with NO `//` authority marker, e.g. `com.wizemann.herald.compat140:/callback` (a `scheme://host` form like `herald://callback` or `com.wizemann.herald://callback` is rejected with `invalid_redirect_uri`). Requested scopes `mail:read mail:write mail:send offline_access`; token exchange succeeded; GET /api/v1/messages?includeLabels=true with the bearer → 200, returns a bare JSON array (100 rows) whose objects have no `labels` key (keys: id, threadId, mailboxId, direction, folder, fromAddress, fromName, to, subject, snippet, receivedAt, sentAt, readAt, starredAt, hasAttachments, createdAt) — confirms `includeLabels` is silently ignored at 1.4.0 rather than erroring. #curl-pkce-140
- [gotcha] Same isolation rule as 1.4.2 applies: never check out v1.4.0 in ~/Developer/hqbase itself (shared checkout, currently on `feat/hqbase-domain-move`); always use `git worktree add`. Both test worktrees (hqbase-142 on 8787, hqbase-140 on 8788) are left running in the background after this session. #isolation-140
