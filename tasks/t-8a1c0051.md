---
id: t-8a1c0051
title: Verify journal-mode sync live once an HQBase release includes #35/#37; retire the 100-cap guard note
status: done
added: 2026-08-18
priority: high
---

## Description

See roadmap notes (session close 2026-08-18).

## Plan



## Artifacts

Verification DONE 2026-09-04 inside P6 (t-063dbcb0), against the live local 1.3.4 instance:
- checkpoint (cursor-less) → `{"changes":[],"nextCursor":…,"hasMore":false}`, then a star mutation produced exactly one `upsert` entry carrying a full MessageSummary, `hasMore:false`. The shape SyncEngine.bootstrap/consumeChanges assumes is confirmed.
- `GET /messages?limit=2` returns a real `Link: <…cursor=…>; rel="next"` header, so 1.3.4 pagination is present.
- 100-cap guard: NOT retired, and reconciled honestly instead. It is now the legacy-server path only — `paginatingAccounts` flips on the first `Link` seen and the guard stops applying; removing it would erase unreturned mail on every pass against a pre-1.3 server. Recorded in the Herald Sync Model note (#cap-status).
- Found and fixed a real defect on the way: a foreign/out-of-range cursor answers 400 INVALID_CHANGE_CURSOR, not 410, and was therefore permanently unrecoverable (the cursor is persisted in the cache). Now mapped to `.cursorExpired` → re-bootstrap.

Only unverified corner: the 410 CHANGE_CURSOR_EXPIRED path cannot be exercised live — the server reserves it for a retention policy it has not built yet. Covered by fixture tests.

