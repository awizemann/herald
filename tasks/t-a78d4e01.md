---
id: t-a78d4e01
title: P1 Multi-account: per-account graphs in AppEnvironment + sidebar account switcher
status: done
added: 2026-08-18
priority: high
---

## Description

Replace the single account/syncEngine/mail/outbox fields in Herald/App/AppEnvironment.swift (`accounts.first` at ~:98, teardownGraph tearing down the first account when a second arrives) with per-account graphs keyed by Account.ID plus a persisted selectedAccountID. Sidebar account switcher; signOut/reauthenticate scoped to the selected account; compose view models keyed by account; unread aggregation across accounts. Model/store/sync are already per-account (accountID on every SwiftData model, SyncEngine per account, per-account tokens/DCR). Branch: feat/multi-account. Reference: documents/reports/feature-investigation-2026-08-18.md §4.

## Plan



## Artifacts

Merged to main (feat/multi-account, 1b33b26). App tests 79→87, kit 157. Leftovers: re-auth still discards that account's open composers; account unreachable at launch is absent from switcher until relaunch.

