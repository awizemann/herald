---
id: t-9f94365e
title: Split MailStore.swift (1254) and MailViewModel.swift (1183) under the 1000-line limit
status: done
added: 2026-08-18
---

## Description

Standards audit 2026-08-18 (04 Swift Conventions §4), Medium. Two production files exceed the ~1000-line service limit: HeraldKit/Sources/HeraldKit/Sync/MailStore.swift (1254) and Herald/App/MailViewModel.swift (1183). Both are the app's two hotspots. Extract cohesive slices into `@MainActor enum` static-method helpers (the standard's extraction pattern) — e.g. pull the local-action/undo materialisation and the journal upsert/fence logic out of MailStore; pull row-presentation/reload-instrumentation helpers out of MailViewModel. Keep behavior identical; suite must stay green (remember `xcodegen generate` before `xcodebuild test` if files are added). Near-threshold, not egregious.

## Plan

PHASE 4 of standards remediation (2026-08-18). Split the two over-limit files under 1000 lines via @MainActor enum static-helper extraction (standard 04 §4). Both HeraldKit (SwiftPM) and Herald (xcodegen dir-glob) auto-include new files — no manifest edits; run xcodegen generate before xcodebuild.
Run as TWO sub-agent steps:
  4a MailStore.swift (1254) — extract cohesive slices (e.g. local-action/undo materialisation; journal upsert + pending-mutation fence) into helper files in HeraldKit/Sources/HeraldKit/Sync/. Behavior identical.
  4b MailViewModel.swift (1183) — extract row-presentation / reload-instrumentation / action helpers into files under Herald/App/. Behavior identical.
Pure mechanical extraction — no logic changes. Gate (each): xcodegen generate; xcodebuild -scheme Herald test + HeraldKit swift test green; confirm both target files now <1000 lines; self-audit. No commit, no managed-tier edits.

## Artifacts



