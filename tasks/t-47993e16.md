---
id: t-47993e16
title: Low-risk audit hygiene: comment try? decodes, fix TOCTOU, log revert failures, drop constant force-unwrap
status: done
added: 2026-08-18
priority: low
---

## Description

Standards audit 2026-08-18 (04/03/02), Low — batch of small, safe cleanups:
- Add explanatory comments to the 4 bare `try? decode` sites (all inside guard-let OAuth error/token branches): OAuthHTTP.swift:90, OAuthSession.swift:139, DynamicClientRegistration.swift:46 and :53. Standard 04 §3 requires a comment on ignorable try?.
- Replace the TOCTOU `fileExists`→`removeItem` at MailStoreContainer.swift:97-99 with a commented `try? removeItem` (ENOENT is already caught/harmless on a rebuildable cache).
- Log the swallowed optimistic-revert failures at MailActionService.swift:28, 70, 83 rather than fully discarding them.
- Replace the constant force-unwrap `URL(string: "com.wizemann.herald:/oauth/callback")!` at DynamicClientRegistration.swift:13 with a documented static / guard-let (nil risk is zero, but removes the `!`).
Leave the Sparkle ObservableObject/@Published + import Combine bridge (UpdateService.swift:6,153-154) as-is — no @Observable equivalent for Sparkle's KVO publisher; it is a sanctioned interop point.

## Plan

PHASE 1 of standards remediation (2026-08-18), sequential, commit on main. Smallest/isolated — runs first to validate the build+test loop.
Sub-agent (Opus) fixes, exact sites:
- OAuthHTTP.swift:90, OAuthSession.swift:139, DynamicClientRegistration.swift:46 & :53 — add a one-line comment on each bare `try? decode` explaining why the failure is ignorable (all sit in guard-let error/token branches).
- MailStoreContainer.swift:97-99 — drop the `fileExists` check; use a commented `try? removeItem` (ENOENT is harmless on a rebuildable cache).
- MailActionService.swift:28, 70, 83 — log the optimistic-revert failure (logger.warning) instead of silently swallowing.
- DynamicClientRegistration.swift:13 — replace `URL(string: "com.wizemann.herald:/oauth/callback")!` with a guard-let/static (nil risk zero; removes the force-unwrap).
- LEAVE the Sparkle ObservableObject/@Published + import Combine bridge (UpdateService.swift) as-is — sanctioned interop.
Gate: `xcodegen generate` if needed; `xcodebuild -project Herald.xcodeproj -scheme Herald -configuration Debug -destination 'platform=macOS' -derivedDataPath DerivedData test` + `cd HeraldKit && swift test` — all green. Self fresh-eyes audit. Do NOT commit (orchestrator audits+commits). Do NOT touch managed tiers (.memory/, documents/, TASKS.md, tasks/).

## Artifacts



