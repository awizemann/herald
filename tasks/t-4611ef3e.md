---
id: t-4611ef3e
title: P1: re-vendor spec @1.3.4 + regen client; adopt restore/unarchive, POST /forward (fixes forward-content-loss bug), multipart Content-Type
status: done
added: 2026-09-04
priority: high
---

## Description

Spec diff + findings: documents/plans/investigation-2026-09-04-upstream-134-and-client-defects.md. Forward bug: SendInput carries no forward link; sending before autosave persists a server draft loses content (OutboxService.swift:225, ComposePrefill.swift:179). Supersedes t-8a1c0050 (Put back).

## Plan



## Artifacts

P1 landed on `feat/upstream-134-adoption` as commit `0893dc9`
(feat(api): adopt upstream 1.3.4 — restore/unarchive, POST /forward, multipart Content-Type).

Delivered
- Spec re-vendored at upstream v1.3.4 via `scripts/vendor-openapi.py`; client regenerated.
- `restore` / `unarchive` on both action enums; Trash gets "Put Back", Archive gets
  "Move to Inbox" (toolbar, reading-pane header, list context menu, VoiceOver rotor,
  ⌘⇧A and the `e` key). Closes the old t-8a1c0050 / issues #7, #8. The per-message
  archive-from-Trash hack is gone.
- `POST /forward` replaces forward-via-`POST /send` — fixes the forward-content-loss
  bug. Herald now requires a server ≥ 1.3.4 to forward (older answers 404 and the
  compose window keeps the draft).
- Draft attachment upload sends a real per-part Content-Type (raw multipart part; the
  generated typed part can only emit the spec's literal `*/*`).
- Picked up from the re-vendor: nullable `MessageChange.delete.mailboxId` (code change),
  plus required `Attachment.disposition`, `DraftAttachment.inline`, `Draft.signature`/
  `labels`, three `MessageHtml` remote-image flags (fixture updates only).
- New shared `Herald/Views/TriageButtons.swift` — the toolbar and reading-pane header
  had drifted; the reading pane was still offering the Trash no-op archive.

Tests: 382 pass (173 app, 209 HeraldKit). New wire-level `Upstream134AdoptionTests`,
store-level restore-destination coverage, a null-mailbox tombstone through SyncEngine,
multipart Content-Type assertions.

Memory: appended a verified 1.3.4 section to
`.memory/architecture/HQBase Mail API v1 Contract.md`.

Deferred (later phases, all now visible in the vendored spec)
- `MessageHtml.afterQuotedHtml` is ingested but never rendered → authored text BELOW a
  quote is silently dropped today. Fold into P4.
- `Attachment.disposition` should replace Herald's inline inference (P3).
- `Draft.signature` / `Draft.labels` / `DraftAttachment.inline` unmapped (labels &
  signatures roadmap).
- Message vs conversation route disagree on the catchall predicate (`is_unassigned` vs
  `mailbox_id IS NULL`); Herald caches only the mailbox id. Comment left at the site.

