---
id: t-8a1c0003
title: P0.3 Sync + store: SwiftData cache, MailStore @ModelActor, SyncEngine actor, diff tests
status: done
added: 2026-08-15
priority: high
---

## Description

Rebuildable-cache SwiftData models (Mailbox, Conversation, Message), MailStore maps to DTOs, SyncEngine polls conversations (paged) + messages per folder, diffs, writes only changes, publishes AsyncStream<SyncEvent>. Discriminating tests: unchanged rows produce no writes; state timestamp changes do.

## Plan

