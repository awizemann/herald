---
id: t-d60a1704
title: P4: reading formatting — quotedHTML collapse, dark-mode body normalization, blockquote hierarchy, plain-text linkify
status: done
added: 2026-09-04
---

## Description

quotedHTML already delivered by server and discarded (MailViewModel.swift:1466) — collapse toggle is highest-leverage. NEW from P1: `MessageHtml.afterQuotedHtml` (authored text BELOW the quote) is ingested but never rendered — live content-loss bug, must be fixed here; also `htmlHasRemoteImages`/`quotedHtmlHasRemoteImages`/`afterQuotedHtmlHasRemoteImages` flags now available for smarter remote-media gating. Merge/extend t-8a1c0017. Stacked thread view deferred (needs JS height reporting; JS disabled). See plan §C.

## Plan



## Artifacts

Commit c55fd8f on feat/upstream-134-adoption.

Landed:
- `MessageHTML` carries `afterQuotedHTML` + per-fragment remote-image flags (HeraldKit Model/Mapping); `remoteImagesAreOnlyInQuotedHistory` words the consent banner.
- `MailViewModel.composeBody` renders all three fragments; quoted history in a native `<details>` (JS stays off), fragments stripped of their own details/summary tags.
- Dark-mode normalization from new `MailTheme.Web` tokens mirrored into CSS custom properties (no inversion of sender colours); nested blockquote bars/wash; `section.body { overflow-x: auto }` for wide tables.
- text/plain: system font, URL linkification (escape-as-emitted, http(s) only, deceptive `user@host` authorities left inert), tinted `>` lines, trailing quoted history folded into the same disclosure — covers t-8a1c0017 in full.
- Offline cached-body path keeps the reader's remote-media consent; the cache stores the COMPOSED fragment so offline reads keep the quote and after-quote text.

Tests: new `HeraldTests/MessageBodyCompositionTests.swift` (13 tests). Full suites green — 212 app tests / 216 HeraldKit tests; `./scripts/build-detached.sh` succeeds and launches.

Memory: decisions/Herald Reading-Pane Document Composition; conventions/Herald Design System and Accessibility (2026-09-04 update).

Deferred: stacked-thread view (still needs JS height reporting); CSP `img-src http:` after consent and `media-src` not widening with consent (pre-existing, flagged in review); caching presentation markup has no render-format version on the sidecar (cache is rebuildable).

