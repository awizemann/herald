---
id: t-e425c325
title: Analytics P1: swift-stats package + Analytics module + wire tests
status: done
added: 2026-08-18
priority: high
---

## Description

Add swift-stats (from 0.1.0; products Stats, StatsCloudflare; StatsTesting for tests) to project.yml (packages block) and regenerate with xcodegen. Create Herald/Analytics/UsageEvent.swift (enum, one case per approved event, name + props: [String: UsageValue]; closed vocabularies; error-kind mappers for MailAPIError/OAuthError/OutboxError via explicit switch), Herald/Analytics/UsageTracking.swift (nonisolated protocol UsageTracking: Sendable {track/applicationDidBecomeActive/flush/setEnabled/isEnabled}, StatsUsageTracker wrapping StatsClient (consent .all, autoEvents [.appOpen,.sessions], installIdSalt "herald-mac-2026", CloudflareSink → https://api.swiftstats.co), NoopUsageTracker, UsageAnalytics.makeTracker(environment:arguments:writeKey:) gated off under XCTest / demo-mock flags / missing-empty-$(...) key). Add Herald/PrivacyInfo.xcprivacy (Product Interaction + Other Diagnostic Data, not linked, not tracking, NSPrivacyTracking false, UserDefaults CA92.1) and ensure it's in the app bundle resources.
Blast radius: project.yml, Herald.xcodeproj (regen), new Herald/Analytics/*, new PrivacyInfo.xcprivacy, HeraldTests/Analytics*.
Tests (Swift Testing, StatsTesting): name regex + not reserved + no stats_ prefix; every string prop in closed vocab; exhaustive switch (no default) over UsageEvent; push all events through real StatsClient+InMemorySink with FixedUUIDProvider/FixedRandomSource + temp storageDirectory, assert order+props; makeTracker gating table; error-kind mapping never contains payload text.
Approved event list + vocab: see documents/plans/usage-analytics-plan.md.

## Plan



## Artifacts

Herald/Analytics/UsageEvent.swift, UsageTracking.swift, Herald/PrivacyInfo.xcprivacy, HeraldTests/Analytics/* (147 tests green). Deviations: consent set via StatsConfiguration.consent (.all) not lazy setConsent (would re-widen narrowed consent); no demo/mock flags exist in Herald; per-type error-kind enums. Gotcha: swift-stats UserDefaults suite keyed by appId → tests need unique appId.

