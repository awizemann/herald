---
id: t-197c4c10
title: Analytics P5: keyed E2E run, fresh-eyes audit, commit, record decisions
status: done
added: 2026-08-18
priority: high
---

## Description

Fetch write key via get_vendor_credential (swift-stats) to a temp file outside repo; keyed local build via temp -xcconfig; launch; drive actions; confirm SDK identity suite seq advanced and queue file emptied (202), no drop/refusal logs. Opus fresh-eyes audit: privacy leaks / concurrency / didSet double-firing / key plumbing / checkbox tests. Fix all. Commit (no push). Record decisions in Memophant.

Progress 2026-08-18: E2E DONE — keyed build, real install UUID, seq 4→7, queue drained to 0 bytes (202), events verified: sync_completed{trigger,changed}, compose_opened{kind:new}; no SDK error/drop logs. Audit blocker found+fixed: Info.plist is xcodegen-generated → HeraldStatsWriteKey now declared in project.yml info.properties (reference only; verified env export bakes nothing). Audit items 2–11 being applied by fix-up agent. Gotcha: xcodebuild test in same derivedData rebuilds the app without the key.

## Plan



## Artifacts

Commit b9bad49. E2E verified against api.swiftstats.co (seq advanced, queue drained on 202). Opus audit: 1 blocker (plist generated by xcodegen) + 10 items — all fixed; detached-shell test sweeper replaced with in-process stale sweep. 172 HeraldTests green. Decision note: decisions/Herald Usage Analytics (swift-stats). Not pushed.

