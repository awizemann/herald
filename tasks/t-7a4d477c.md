---
id: t-7a4d477c
title: Fix: usage-stats (analytics) opt-out toggle appears to be a no-op
status: done
added: 2026-09-04
priority: high
---

## Description

DIAGNOSED 2026-09-04: unkeyed builds (no APP_STATS_WRITE_KEY — dev/CI/tests) get NoopUsageTracker (UsageTracking.swift:163-190) whose setEnabled is empty and isEnabled always false → Settings switch renders live-but-off and snaps back when flipped = the reported no-op. Keyed builds are correct (SDK gates per-event, setEnabled(false) drops queue) but give no opt-out feedback. FIX (after P8 lands): isAvailable flag on the UsageTracking seam; Privacy pane disables the switch with "not configured in this build" caption + a11y hint; optional keyed-build confirmation cue. ~15 lines in Herald/Analytics/UsageTracking.swift + Herald/Views/SettingsView.swift.

## Plan

Fixed 2026-09-04, commit 6c4987a on feat/upstream-134-adoption (not pushed).

- Added `isAvailable` to the UsageTracking seam (false on NoopUsageTracker, true on StatsUsageTracker).
- PrivacySettingsPane/UsagePrivacyModel: toggle now disables and shows "Usage analytics aren't included in this build" (caption + a11y hint) when unavailable, instead of looking like a live-but-broken switch.
- Keyed builds now show a transient confirmation caption ("Saved." / "Analytics are off — nothing is sent.") after a successful setEnabled.
- Extended HeraldTests/Analytics/UsagePrivacySettingsTests.swift with 2 new tests (unavailable-build behavior, keyed-build confirmation); added RecordingUsageTracker.makeUnavailable() test helper.
- All 252 app tests pass (xcodebuild test), HeraldKit `swift test` passes (252 tests/32 suites), build-detached.sh succeeded.

Left status as "doing" per instructions — ready for review/close.

## Artifacts



