<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Herald</title>
        <item>
            <title>0.4.0</title>
            <pubDate>Fri, 04 Sep 2026 19:36:50 -0400</pubDate>
            <sparkle:version>8</sparkle:version>
            <sparkle:shortVersionString>0.4.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<p>Herald now targets HQBase 1.3.4 or newer: forwarding and message rendering use API surfaces that older servers don&#x27;t have.</p>
<h3>Added</h3>
<ul>
<li>Put Back. Trashed conversations can be restored (&quot;Put Back&quot;) and archived ones moved back to the inbox — toolbar, context menu, ⌘⇧A, the <code>e</code> key and VoiceOver all follow one folder rule, so no more silent no-op archive from the Trash.</li>
<li>Labels. Workspace labels sync into a sidebar section with per-label listings, colored name chips on rows and in the reading pane, and add/remove from the context menu, the reading-pane header, and the Message menu. Label colors match the web app.</li>
<li>Signatures. Compose shows the signatures usable from the sending address, picks the address&#x27;s default automatically, and lets you switch (or send with none). The server appends the signature, and a read-only preview shows exactly what will go out. A draft&#x27;s saved signature is kept when you reopen it.</li>
<li>Quoted-thread preview in compose. Reply and forward windows show a collapsed, read-only preview of the original the server will quote below your text.</li>
<li>Instant sync. Herald now holds a lightweight wake connection to the server and refreshes the moment something changes, instead of waiting for the next poll. Polling remains as a fallback, stretched while the connection is healthy.</li>
<li>Automatic sign-in repair. When a session expires while your HQBase web session is still alive, Herald re-runs the sign-in by itself the next time it&#x27;s frontmost — the window flashes once and syncing resumes. The banner stays as a fallback and shows progress.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Forwarding uses the server&#x27;s dedicated forward route, so the forwarded message can no longer be lost by sending before an autosave; the original&#x27;s attachments ride along.</li>
<li>Message rendering: text a sender wrote below the quoted history is now shown (it was silently dropped); quoted history collapses behind &quot;Show quoted history&quot;; nested quotes get per-level bars; plain-text mail renders in the system font with clickable links and tinted quote lines; wide tables scroll inside the message instead of the whole pane; dark mode no longer glares on unstyled light emails, and Increase Contrast is honored inside the message view.</li>
<li>Attachments: downloads carry their real file type (previews and Quick Look work for files the server sends untyped or misnamed), inline-vs-attachment now follows the server&#x27;s intent, the attachment bar survives offline reads, saving reuses the already-downloaded file, and a file being previewed or dragged can no longer be evicted mid-use. Failed inline images are called out instead of leaving silent holes.</li>
<li>Sending marks messages read/unread, stars, archives and trashes more reliably from the Trash and Archive scopes, which the server treats specially.</li>
<li>The Privacy pane&#x27;s usage-statistics toggle now says when analytics aren&#x27;t included in a build (dev copies) instead of rendering a switch that snaps back, and confirms opt-out in real builds.</li>
<li>Accessibility: selection state in the signature menu, contrast-safe label chips, VoiceOver announcements for session-repair and blocked/failed image banners, and a Labels submenu in the Message menu.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Sessions no longer log out when two Herald copies share one account (server-side fix Herald asked for in HQBase 1.3.x, plus client-side hardening).</li>
<li>A rejected sync cursor now recovers by re-syncing instead of failing forever.</li>
<li>Cached data written by an older Herald can no longer crash the app on read; the cache degrades and heals on the next sync.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/awizemann/herald/releases/download/v0.4.0/Herald-0.4.0.zip" length="3563798" type="application/octet-stream" sparkle:edSignature="o3funPV1vjHNqCsJTms//1BVX1m9bHpjdsGja5lQHwStJ+ewzyswYI4Bvw6oHM9fa2DSgVGBEze1L9YApHEwAw=="/>
        </item>
    </channel>
</rss>