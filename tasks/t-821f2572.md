---
id: t-821f2572
title: A4: security hardening — refuse ws:// downgrade, throw on empty socket token, stream OAuth response cap
status: done
added: 2026-09-04
---

## Description

From documents/reports/full-surface-audit-2026-09-04.md (security §): URLSessionMailEventChannel.eventsURL drops the http→ws arm (return nil); open(token:) throws on empty token (tests use MailEventChannelOpening seam); OAuthHTTP streams via session.bytes with the size cap enforced during download; add mimeType shape guard at inline-image assembly; clamp staged filenames by UTF-8 bytes not Characters.

## Plan

Committed as 26d99e7 on feat/upstream-134-adoption, staging only my 9 files (verified via git status before commit — the concurrent agent's SyncEngine.swift, MailStore+Labels.swift, MailViewModel+Labels.swift, SidebarView.swift, and its related test edits were left untouched/unstaged).

Changes:
1. HeraldKit/Sources/HeraldKit/Sync/URLSessionMailEventChannel.swift — eventsURL refuses http/ws (nil); open(token:) throws .unauthorized on empty token; WebSocketChannel made internal so the live-test suite gets its own cookie-session opener.
2. HeraldKit/Sources/HeraldKit/Auth/OAuthHTTP.swift — send(_:using:) streams via session.bytes(for:), aborting past maxResponseBytes during download.
3. Herald/App/MailViewModel+HTMLAssembly.swift — added hasValidMIMETypeShape, enforced before the image/video/audio prefix check in isRenderableInlineMedia.
4. Herald/Support/AttachmentFile.swift — added clampedToUTF8Bytes(_:limit:); filename(for:contentType:) clamps by UTF-8 bytes (255) not Characters.

Tests added: HeraldKit/Tests/HeraldKitTests/Sync/URLSessionMailEventChannelTests.swift (new), OAuthSessionTests.oversizedTokenResponseIsRejected, LiveEventSocketTests CookieSessionEventChannelOpener + conditional token/cookie path, MessageRenderingSecurityTests.malformedMimeTypeShapeIsRejectedDespiteMediaPrefix, AttachmentHandlingTests multi-byte byte-clamp tests.

Verification: `swift test` in HeraldKit — 285/285 pass (hit repeated transient compile breaks in MailStore+Labels.swift/SyncEngine.swift from the concurrent agent's in-flight edits; cleared on retry each time, confirmed unrelated to my files). `xcodebuild test` for the Herald scheme — the HeraldTests bundle's 285 tests (including all new/edited ones, e.g. saw "oauth response too large" log line from my new oversized-response test) all passed; a separate second test-host launch crashed with SIGABRT, traced via the crash log (~/Library/Logs/DiagnosticReports) to a `Stats.framework` code-signature Team-ID mismatch from concurrent xcodebuild DerivedData writes — unrelated to my diff. `./scripts/build-detached.sh` — BUILD SUCCEEDED, app launched. `git checkout HeraldKit/Package.resolved` run twice (once before, once after the detached build regenerated it) to keep it out of the commit.

Self-audit: diff is scoped exactly to the 4 source files named in the task plus their tests; no managed-tier files or other agent's files staged/committed.

## Artifacts



