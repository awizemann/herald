---
id: t-16dd066e
title: P2: auto minimal reauth — quiet reauthenticate variant, auto-trigger on needsReauth, banner fallback
status: done
added: 2026-09-04
priority: high
---

## Description

Hook points: MailViewModel.swift:1092 (before banner) or AccountTokenProvider.swift:131 escalation; quieter performSignIn (AppEnvironment.swift:552-557). Rate-limit; only when app active; fall back to ReauthBanner on cancel/failure. See documents/plans/investigation-2026-09-04-upstream-134-and-client-defects.md §A.

## Plan



## Artifacts

Commit cc61d02 on feat/upstream-134-adoption (code only; managed tiers left dirty).

New: Herald/App/AutoReauthPolicy.swift (frontmost / one-in-flight / 10-min cooldown, per account), HeraldTests/AutoReauthPolicyTests.swift.
Changed: AppEnvironment (quiet performSignIn via isAutomatic, SignInResult carrying the account, attemptAutomaticReauthentication + retryAutomaticReauthentication on activation & selection change, sign-out cancels an in-flight attempt, isApplicationActive seam), MailViewModel (reauthenticationRequired fired on the TRANSITION into .needsReauth), RootView (banner shows "Signing you back in…" + spinner, withdraws the button), UsageEvent (account_reauthenticated gains an `automatic` prop).
Tests: 4 new AppEnvironment integration tests (background deferral + retry on activation, cooldown, background-account scoping, sign-out cancels), 2 new MailViewModel tests, 6 policy unit tests. Full app suite (185) + HeraldKit (209, swift test and Xcode) green; build-detached OK. The deferral test was verified to fail with the activation retrigger removed.

Memory: decisions/Automatic Re-auth Policy (frontmost, deferred, rate-limited); appended to architecture/HQBase Mail API v1 Contract.

Deferred: no watchdog/timeout on a stuck consent window (banner shows the spinner until the window is closed); no test asserts the in-flight `true` banner state end-to-end (would need an OAuth URLProtocol fake ported into the app test target — the three tests using https://127.0.0.1:9 rely on a fast connection refusal); t-8a1c0052 (banner above account header) needs no change — the banner is a top safeAreaInset on the whole split view and already stacks above the sidebar header rather than replacing it.

